apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    annot4: value4
  labels:
    label2: value2
  name: ibm-lsr-console2
  namespace: reporter
spec:
  path: /license-service-reporter
  port:
    targetPort: proxy
  tls:
    insecureEdgeTerminationPolicy: None
    termination: reencrypt
  to:
    kind: Service
    name: ibm-license-service-reporter2
    weight: 100
  wildcardPolicy: None
